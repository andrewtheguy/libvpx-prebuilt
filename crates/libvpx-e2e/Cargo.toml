[package]
name = "libvpx-e2e"
version.workspace = true
edition = "2021"
description = "Encodes synthetic screen content with the prebuilt libvpx and decodes it back, so the pipeline proves the archive works rather than that it links"
license = "BSD-3-Clause"
publish = false

[dependencies]
vpx-sys = { package = "libvpx-prebuilt-sys", path = "../libvpx-prebuilt-sys" }
